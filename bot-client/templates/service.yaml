apiVersion: v1
kind: Service
metadata:
  name: {{ include "bot-client.fullname" . }}
  labels:
    {{- include "bot-client.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bot-client.selectorLabels" . | nindent 4 }}
